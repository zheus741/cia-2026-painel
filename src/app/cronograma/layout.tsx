import { AppShell } from '@/components/app-shell'

export default function CronogramaLayout({ children }: { children: React.ReactNode }) {
  return <AppShell section="Cronograma">{children}</AppShell>
}
