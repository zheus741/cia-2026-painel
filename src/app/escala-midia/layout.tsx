import { AppShell } from '@/components/app-shell'

export default function EscalaMidiaLayout({ children }: { children: React.ReactNode }) {
  return <AppShell section="Escala da Equipe">{children}</AppShell>
}
