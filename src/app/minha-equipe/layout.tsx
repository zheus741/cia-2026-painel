import { AppShell } from '@/components/app-shell'

export default function MinhaEquipeLayout({ children }: { children: React.ReactNode }) {
  return <AppShell section="Minha Equipe">{children}</AppShell>
}
