import { AppShell } from '@/components/app-shell'

export default function MinhaEscalaLayout({ children }: { children: React.ReactNode }) {
  return <AppShell section="Minha Escala">{children}</AppShell>
}
