import { AppShell } from '@/components/app-shell'

export default function PlacarLayout({ children }: { children: React.ReactNode }) {
  return <AppShell section="Placar Ao Vivo">{children}</AppShell>
}
