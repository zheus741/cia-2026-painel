import { AppShell } from '@/components/app-shell'

export default function RedesLayout({ children }: { children: React.ReactNode }) {
  return <AppShell section="Redes Sociais">{children}</AppShell>
}
